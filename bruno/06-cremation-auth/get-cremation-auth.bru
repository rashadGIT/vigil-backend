meta {
  name: Get Cremation Auth
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/cases/{{caseId}}/cremation-auth
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
