meta {
  name: Convert Preneed to Case
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/preneed/{{preneedId}}/convert
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status is 200 or 201", function() {
    expect([200, 201]).to.include(res.getStatus());
  });
}
