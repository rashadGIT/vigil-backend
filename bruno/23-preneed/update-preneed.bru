meta {
  name: Update Preneed
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/preneed/{{preneedId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "clientPhone": "555-0499",
    "notes": "Client reviewed and approved arrangements"
  }
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
