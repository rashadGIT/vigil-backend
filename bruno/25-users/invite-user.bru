meta {
  name: Invite User
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "email": "newstaff@demo-tenant.com",
    "name": "Sam Rivera",
    "role": "staff"
  }
}

tests {
  test("status is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
