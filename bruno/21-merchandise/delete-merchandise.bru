meta {
  name: Delete Merchandise Item
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/merchandise/{{merchandiseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
