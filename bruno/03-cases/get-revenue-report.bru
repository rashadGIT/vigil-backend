meta {
  name: Get Revenue Report
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/cases/reports/revenue
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  ~from: 2024-01-01
  ~to: 2024-12-31
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
