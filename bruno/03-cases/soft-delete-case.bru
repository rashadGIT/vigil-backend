meta {
  name: Soft Delete Case
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/cases/{{caseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
