meta {
  name: Update First Call
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/cases/{{caseId}}/first-call
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "authorizationMethod": "written",
    "specialInstructions": "Embalming requested"
  }
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
