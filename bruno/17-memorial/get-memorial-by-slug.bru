meta {
  name: Get Memorial by Slug (Public)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/memorial/{{memorialSlug}}
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
