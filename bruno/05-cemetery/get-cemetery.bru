meta {
  name: Get Cemetery
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/cases/{{caseId}}/cemetery
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
