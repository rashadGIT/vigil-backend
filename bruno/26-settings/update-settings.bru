meta {
  name: Update Settings
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/settings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Demo Funeral Home",
    "googleReviewUrl": "https://g.page/r/demo-funeral-home/review"
  }
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
