meta {
  name: Delete Vendor
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/vendors/{{vendorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
