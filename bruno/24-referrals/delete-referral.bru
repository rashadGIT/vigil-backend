meta {
  name: Delete Referral
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/referrals/{{referralId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
