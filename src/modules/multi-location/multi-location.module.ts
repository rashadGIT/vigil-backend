import { Module } from '@nestjs/common';

/**
 * Phase 3 stub — Multi-location ownership groups.
 * Schema model `Location` already exists (Phase 4).
 * Implementation deferred to Phase 3 feature work.
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
})
export class MultiLocationModule {}
