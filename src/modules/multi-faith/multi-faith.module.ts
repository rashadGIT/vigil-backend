import { Module } from '@nestjs/common';

/**
 * Phase 3 stub — Faith tradition templates.
 * Schema model `FaithTraditionTemplate` already exists (Phase 4).
 * Implementation deferred to Phase 3 feature work.
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
})
export class MultiFaithModule {}
